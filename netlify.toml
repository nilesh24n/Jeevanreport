[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = ".next"

[dev]
  command = "npm run dev"
  port = 3000

[context.production]
  environment = { NODE_VERSION = "18.17.0" }
